import { NextResponse } from 'next/server';import { getCurrentUser,getWorkspaceForUser } from '@/lib/auth';import { approveCompetitor } from '@/lib/intelligence';
export async function POST(request){try{const user=await getCurrentUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const workspace=await getWorkspaceForUser(user.id);const body=await request.json();await approveCompetitor({workspaceId:workspace.id,relationshipId:body.relationshipId,action:body.action,classification:body.classification});return NextResponse.json({ok:true});}catch(error){return NextResponse.json({error:error.message},{status:400});}}
