import {NextResponse} from 'next/server';
import {getCurrentUser,getWorkspaceForUser} from '@/lib/auth';
import {generateDecision,updateDecision} from '@/lib/decisions';
import {rateLimit} from '@/lib/rateLimit';
export async function POST(request){try{const user=await getCurrentUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});await rateLimit(`decision:${user.id}`,{limit:20,windowSeconds:3600});const workspace=await getWorkspaceForUser(user.id);if(!workspace)return NextResponse.json({error:'Workspace missing'},{status:400});const body=await request.json();const decision=await generateDecision(workspace.id,String(body.moveId||''));return NextResponse.json({ok:true,decision});}catch(error){return NextResponse.json({error:error.message||'Could not build decision'},{status:400});}}
export async function PATCH(request){try{const user=await getCurrentUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const workspace=await getWorkspaceForUser(user.id);if(!workspace)return NextResponse.json({error:'Workspace missing'},{status:400});const body=await request.json();const decision=await updateDecision({workspaceId:workspace.id,userId:user.id,decisionId:String(body.decisionId||''),status:String(body.status||''),decision:body.decision});return NextResponse.json({ok:true,decision});}catch(error){return NextResponse.json({error:error.message||'Could not update decision'},{status:400});}}
