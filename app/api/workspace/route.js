import {NextResponse} from 'next/server';import {getCurrentUser,getWorkspaceForUser} from '@/lib/auth';import {query} from '@/lib/db';
export async function DELETE(request){try{const user=await getCurrentUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const workspace=await getWorkspaceForUser(user.id);if(!workspace)return NextResponse.json({error:'Workspace missing'},{status:404});const body=await request.json().catch(()=>({}));if(String(body.confirm||'')!==`DELETE ${workspace.name}`)return NextResponse.json({error:`Type DELETE ${workspace.name} to confirm.`},{status:400});await query('DELETE FROM workspaces WHERE id=$1 AND EXISTS (SELECT 1 FROM workspace_members WHERE workspace_id=$1 AND user_id=$2 AND role=$3)',[workspace.id,user.id,'owner']);return NextResponse.json({ok:true});}catch(error){return NextResponse.json({error:error.message||'Workspace deletion failed'},{status:500});}}
