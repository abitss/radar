import { NextResponse } from 'next/server';import { getCurrentUser,getWorkspaceForUser } from '@/lib/auth';import { query } from '@/lib/db';
export async function GET(){const user=await getCurrentUser();if(!user)return NextResponse.json({ok:false},{status:401});const workspace=await getWorkspaceForUser(user.id);if(!workspace)return NextResponse.json({ok:true,workspace:false});const [sig,src]=await Promise.all([query('SELECT max(detected_at) last_signal,count(*)::int count FROM signals WHERE workspace_id=$1',[workspace.id]),query(`SELECT count(*) FILTER (WHERE health='healthy')::int healthy,count(*)::int total FROM sources WHERE workspace_id=$1`,[workspace.id])]);return NextResponse.json({ok:true,lastSignal:sig.rows[0].last_signal,signalCount:sig.rows[0].count,sourceHealth:src.rows[0]});}
