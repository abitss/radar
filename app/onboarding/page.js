import { requireUser, getWorkspaceForUser } from '@/lib/auth';import { redirect } from 'next/navigation';import OnboardingForm from '@/components/OnboardingForm';import Brand from '@/components/Brand';
export default async function Onboarding(){const user=await requireUser();const existing=await getWorkspaceForUser(user.id);if(existing)redirect('/dashboard');return <main className="auth-wrap"><section className="auth-card" style={{width:'min(700px,100%)'}}><Brand/><div className="eyebrow">STARTUP INTAKE</div><h1 className="auth-title">Point RADAR at your company.</h1><p className="sub" style={{fontSize:13,marginBottom:22}}>No 40-field onboarding form. Your public website becomes the starting evidence, then RADAR researches the surrounding market.</p><OnboardingForm/></section></main>}
