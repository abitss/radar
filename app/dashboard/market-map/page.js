import { requireWorkspace } from '@/lib/auth';import { dashboardData } from '@/lib/queries';import MarketMap from '@/components/MarketMap';
export default async function MarketMapPage(){const {workspace}=await requireWorkspace();const data=await dashboardData(workspace.id);return <main className="content"><div className="eyebrow">MARKET MAP</div><h1 className="page-title">See the market as relationships.</h1><p className="sub">A strategic relationship view of direct competitors, adjacent players, substitutes and emerging threats. Distance is illustrative; threat and classification are shown explicitly.</p><div style={{marginTop:24}}><MarketMap companies={data.companies}/></div></main>}
