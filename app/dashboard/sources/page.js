import SourceControls from '@/components/SourceControls';import { requireWorkspace } from '@/lib/auth';import { query } from '@/lib/db';import AlertRules from '@/components/AlertRules';import IntegrationForm from '@/components/IntegrationForm';import EmptyState from '@/components/EmptyState';
export default async function Sources(){const {workspace}=await requireWorkspace();const sources=(await query(`SELECT s.*,c.name company_name FROM sources s JOIN companies c ON c.id=s.company_id WHERE s.workspace_id=$1 ORDER BY COALESCE(s.last_checked_at,s.created_at) DESC`,[workspace.id])).rows;const rules=(await query('SELECT * FROM alert_rules WHERE workspace_id=$1 ORDER BY created_at ASC',[workspace.id])).rows;return <main className="content"><div className="eyebrow">SOURCES & ALERTS</div><h1 className="page-title">Control what RADAR watches.</h1><p className="sub">Inspectable source coverage, monitoring health, frequencies and conservative founder alert thresholds.</p><h2 className="section-title">Monitoring coverage</h2>{sources.length?<div className="table-wrap"><table className="table"><thead><tr><th>Company</th><th>Source</th><th>Type</th><th>Health</th><th>Frequency</th><th>Last checked</th><th>Control</th></tr></thead><tbody>{sources.map(s=><tr key={s.id}><td>{s.company_name}</td><td><a href={s.url} target="_blank" rel="noreferrer">{s.title||s.url}</a></td><td>{s.source_type}</td><td><span className={`health-dot ${s.health}`}/>{s.health}</td><td>{s.check_frequency_minutes} min</td><td>{s.last_checked_at?new Date(s.last_checked_at).toLocaleString():'Baseline only'}</td><td><SourceControls source={s}/></td></tr>)}</tbody></table></div>:<EmptyState title="No sources registered yet" body="Approve a competitor and RADAR will discover strategically relevant public sources."/>}<h2 className="section-title">Alert rules</h2><AlertRules initial={rules}/><h2 className="section-title">Delivery integrations</h2><div className="card"><p className="muted" style={{fontSize:12,lineHeight:1.6}}>Configure Slack, Teams, generic HTTPS webhooks, or a WhatsApp recipient. Endpoints are encrypted at rest. Email alerts use the workspace owner email when Resend is configured.</p><IntegrationForm/></div></main>}
