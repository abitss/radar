import Link from 'next/link';import AuthForm from '@/components/AuthForm';import Brand from '@/components/Brand';
export default function Signup(){return <main className="auth-wrap"><section className="auth-card"><Brand/><div className="eyebrow">CREATE YOUR PRIVATE RADAR</div><h1 className="auth-title">One URL. A living market map.</h1><p className="sub" style={{fontSize:13,marginBottom:22}}>We store your registration date and build an evidence-backed intelligence workspace around your startup.</p><AuthForm mode="signup"/><p className="muted" style={{fontSize:12,textAlign:'center',marginTop:18}}>Already have RADAR? <Link href="/login" style={{color:'#57e8ff'}}>Sign in</Link></p></section></main>}
