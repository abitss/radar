import Link from 'next/link';
import FeedbackButtons from './FeedbackButtons';
export default function SignalCard({signal}){const level=signal.importance>=80?'high':signal.importance>=60?'warn':'cyan';return <div className="signal"><div className="impactbar"/><div><div style={{display:'flex',gap:8,alignItems:'center',flexWrap:'wrap'}}><span className={`badge ${level}`}>{signal.category.replaceAll('_',' ')}</span><span className="muted" style={{fontSize:10}}>{signal.company_name}</span></div><h3 style={{marginTop:8}}>{signal.title}</h3><p>{signal.summary}</p>{signal.impact&&<p style={{marginTop:8,color:'#c5d5e7'}}><strong>Why it matters:</strong> {signal.impact}</p>}{signal.suggested_action&&<p style={{marginTop:6}}><strong style={{color:'#b7ff6a'}}>Review next:</strong> {signal.suggested_action}</p>}<div className="signal-meta"><span className="score">Impact {signal.importance}</span><span className="score">Confidence {signal.confidence}</span><Link className="badge cyan" href={`/dashboard/evidence?signal=${signal.id}`}>Evidence</Link>{signal.fact_or_inference==='inference'&&<span className="badge warn">Inference</span>}</div><FeedbackButtons objectId={signal.id}/></div><div className="muted" style={{fontSize:10,whiteSpace:'nowrap'}}>{new Date(signal.detected_at).toLocaleDateString()}</div></div>}
