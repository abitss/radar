'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function ScanResumeButton({scanId}){const router=useRouter();const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');async function run(){setBusy(true);setMsg('Rebuilding market intelligence…');try{const r=await fetch('/api/scan',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({scanId})});const d=await r.json();if(!r.ok)throw new Error(d.error);setMsg('Scan complete');router.refresh()}catch(e){setMsg(e.message)}finally{setBusy(false)}}return <div style={{marginTop:10}}><button className="button" disabled={busy} onClick={run}>{busy?'Scanning…':'Resume / retry scan'}</button>{msg&&<span className="muted" style={{fontSize:10,marginLeft:8}}>{msg}</span>}</div>}
