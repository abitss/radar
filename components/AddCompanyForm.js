'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function AddCompanyForm(){const router=useRouter();const [open,setOpen]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(e){e.preventDefault();setBusy(true);setError('');const fd=new FormData(e.currentTarget);try{const r=await fetch('/api/companies',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(fd))});const d=await r.json();if(!r.ok)throw new Error(d.error);setOpen(false);router.refresh()}catch(err){setError(err.message)}finally{setBusy(false)}}return <div>{!open?<button className="button primary" onClick={()=>setOpen(true)}>+ Add competitor</button>:<form className="card form" onSubmit={submit} style={{marginTop:12}}><div className="grid grid-3"><div className="field"><label>Company name</label><input className="input" name="name" required/></div><div className="field"><label>Website</label><input className="input" name="website" required placeholder="competitor.com"/></div><div className="field"><label>Classification</label><select className="select" name="classification"><option value="direct">Direct</option><option value="adjacent">Adjacent</option><option value="substitute">Substitute</option><option value="emerging_threat">Emerging threat</option><option value="incumbent">Incumbent</option><option value="watchlist">Watchlist</option></select></div></div>{error&&<div className="error">{error}</div>}<div className="actions"><button className="button primary" disabled={busy}>Add & profile</button><button className="button ghost" type="button" onClick={()=>setOpen(false)}>Cancel</button></div></form>}</div>}
